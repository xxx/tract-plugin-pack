//! Global strip (Recover Sides + Link + Quality). Filled in by Task 16.

use crate::ImagineParams;
use std::sync::Arc;
use tiny_skia::PixmapMut;

pub fn draw(
    _pixmap: &mut PixmapMut<'_>,
    _x: i32,
    _y: i32,
    _w: i32,
    _h: i32,
    _params: &Arc<ImagineParams>,
) {
}
