//! Crossover spectrum view + coherence bar. Filled in by Task 13.

use crate::ImagineParams;
use std::sync::Arc;
use tiny_skia::PixmapMut;

pub fn draw(
    _pixmap: &mut PixmapMut<'_>,
    _x: i32,
    _y: i32,
    _w: i32,
    _h: i32,
    _params: &Arc<ImagineParams>,
) {
}

pub fn draw_coherence(
    _pixmap: &mut PixmapMut<'_>,
    _x: i32,
    _y: i32,
    _w: i32,
    _h: i32,
    _params: &Arc<ImagineParams>,
) {
}
